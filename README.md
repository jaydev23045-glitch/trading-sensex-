# Sensex HFT Scalper

1. Run `npm install`
2. Run `npm run dev` to start dashboard.
3. Upload `vps-server.js` and `vps-setup.sh` to your VPS.
4. Run `bash vps-setup.sh` on VPS.
5. Connect dashboard to VPS via SSH Tunnel (Ports 5000 & 8080).